CarroHtmlRenderer
refactor: extract HTML rendering to CarroHtmlRenderer

refactor: externalize validation messages using ResourceBundle

refactor: implements constantes to CarroValidator

System.out.println(Locale.getDefault());

Você também pode escolher a Locale manualmente
ResourceBundle bundle =
    ResourceBundle.getBundle("messages", Locale.ENGLISH);

ResourceBundle bundle =
    ResourceBundle.getBundle("messages", new Locale("pt", "BR"));

Nesse caso ele ignora a Locale padrão e usa a que você informou.



		HttpServletResponse httpResponse = (HttpServletResponse) response;

		httpResponse.setHeader("X-Content-Type-Options", "nosniff");
		httpResponse.setHeader("X-Frame-Options", "DENY");
		httpResponse.setHeader("Referrer-Policy", "strict-origin-when-cross-origin");



git commit --amend -m "feat: add HTTP security response headers"


git push origin fase-1-crud-jdbc --force-with-lease


<table class="a">
  <tr>
    <th>Seleção</th>
    <th>nome do carro</th>
  </tr>
  <tr>
    <td>Alfreds Futterkiste</td>
    <td>Maria Anders</td>
  </tr>
</table>